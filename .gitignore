# Python bytecode. Two .pyc files were previously committed here and were
# publicly downloadable; .pyc decompiles trivially, and these contained the
# Supabase URL and anon key plus the auth and payment flow.
*.pyc
__pycache__/

# Local scratch
*.tmp
.DS_Store
Thumbs.db
